### For basic setup of server:

    npm i express axios cors dotenv

### For redis:

    npm i redis

### Running redis server:

    redis-server

### Include in .env:

    PORT=3000
    DEFAULT_EXPIRATION=3600